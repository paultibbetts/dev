---
title: config
description: ~/.config
---

import DirectoryTree from "../../../components/DirectoryTree.astro";

The `config` directory is copied over to `~/.config`.

It contains dotfiles for different tools, notably `git`, `tmux`, and `nvim`.

<DirectoryTree path="config" title="/config" maxDepth={1} />
