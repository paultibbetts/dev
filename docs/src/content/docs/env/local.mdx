---
title: local
description: ~/.local
---

import DirectoryTree from "../../../components/DirectoryTree.astro";

The `local` directory is copied over to `~/.local`.

`~/.local/scripts` is added to the `PATH` so executables in that directory are available globally.

<DirectoryTree path="local" title="/local" maxDepth={1} />
